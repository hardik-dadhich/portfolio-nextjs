# Email Service Configuration
# Choose one: 'console' (dev), 'resend', 'sendgrid', 'nodemailer'
EMAIL_SERVICE=console

# Contact Email - where form submissions will be sent
CONTACT_EMAIL=hardikdadhich26@gmail.com

# For Resend (https://resend.com)
# RESEND_API_KEY=your_resend_api_key_here
# FROM_EMAIL=noreply@yourdomain.com

# For SendGrid (https://sendgrid.com)
# SENDGRID_API_KEY=your_sendgrid_api_key_here
# FROM_EMAIL=noreply@yourdomain.com

# For Nodemailer (SMTP)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@email.com
# SMTP_PASS=your_password_or_app_password

# Database Configuration
# SQLite database file path for storing papers and admin users
# The database file will be created automatically if it doesn't exist
DATABASE_URL=file:./database/blog.db

# NextAuth Configuration
# NEXTAUTH_SECRET: A random string used to encrypt JWT tokens and session data
# IMPORTANT: Generate a secure secret with one of these commands:
#   - openssl rand -base64 32
#   - node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Never commit your actual secret to version control!
NEXTAUTH_SECRET=your_nextauth_secret_here_generate_with_openssl_rand_base64_32

# NEXTAUTH_URL: The canonical URL of your site
# For local development: http://localhost:3000
# For production: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000
